import { PERMISSION_CODES } from "../../../shared/permissions/permissionCodes.js";

export const TRANSFER_STATUS = Object.freeze({
  PENDING_APPROVAL: "PENDING_APPROVAL",
  APPROVED: "APPROVED",
  REJECTED: "REJECTED",
  CANCELLED: "CANCELLED",
});

export const TRANSFER_MODE = Object.freeze({
  AMOUNT: "AMOUNT",
  QUANTITY: "QUANTITY",
});

export const TRANSFER_PERMISSIONS = Object.freeze({
  CREATE: PERMISSION_CODES.CREATE_CATEGORY_TRANSFERS,
  APPROVE: PERMISSION_CODES.APPROVE_CATEGORY_TRANSFERS,
});

export const TRANSFER_WORKFLOW_ACTIONS = Object.freeze({
  CREATED: "CATEGORY_TRANSFER_CREATED",
  APPROVED: "CATEGORY_TRANSFER_APPROVED",
  REJECTED: "CATEGORY_TRANSFER_REJECTED",
});
